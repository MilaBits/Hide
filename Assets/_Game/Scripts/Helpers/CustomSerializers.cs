using FishNet.Serializing;

public static class CustomSerializers
{
    //public static void writeTile(this Writer writer, t value)
    //{
    //    writer.Write(value.buildable);
    //    writer.Write(value.Occupied);
    //    writer.WriteVector2Int(value.gridPosition);
    //}

    //public static Tile ReadTile(this Reader reader)
    //{
    //    Tile tile = new Tile();
    //    tile.buildable = reader.ReadBoolean();
    //    tile.occupied = reader.ReadBoolean();
    //    tile.gridPosition = reader.ReadVector2Int();
    //    return tile;
    //}
}
